{
  "scripts": {
    "dev": "native-cli dev",
    "build": "native-cli build",
    "export": "native-cli export",
    "test": "node --experimental-strip-types --test tests/*.test.ts"
  },
  "devDependencies": {
    "@native-sdk/cli": "latest"
  }
}
